import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    message_container:{
        backgroundColor: "#fff",
        height: "100%",
        paddingHorizontal: 16,
        marginTop: 30,
    }
});

export { styles };
